import styled from 'styled-components';

const GenreList = () => {
  return (
    <GenreListWrap className='genrelist'>
     
    </GenreListWrap>
  )
}

export default GenreList

const GenreListWrap = styled.div`
color: var(--clr-red);

  .genrelist{
    color: var(--clr-red);
  }
`;